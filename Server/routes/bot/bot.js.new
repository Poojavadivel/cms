/**
 * routes/bot.js
 * Main entry point for chatbot API routes
 * Refactored for enterprise-grade maintainability and scalability
 * 
 * All business logic has been moved to /bot folder modules:
 * - config.js: Configuration and environment variables
 * - systemPrompts.js: Role-based AI prompts
 * - circuitBreaker.js: Circuit breaker and metrics
 * - aiService.js: OpenAI API integration
 * - intentExtractor.js: Intent and entity extraction
 * - contextBuilder.js: Enhanced context building
 * - entitySearch.js: Patient/staff database search
 * - responseGenerator.js: Response formatting
 * - sessionManager.js: Bot session management
 * - chatController.js: Main chat logic orchestration
 * - routes.js: API route definitions
 * - utils.js: Helper utilities
 */

const botRouter = require('./bot/routes');

module.exports = botRouter;
